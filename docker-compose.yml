services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: idiotic-landing-page
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - caddy-network

networks:
  caddy-network:
    driver: bridge
